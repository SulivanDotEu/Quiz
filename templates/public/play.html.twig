{% extends 'base.html.twig' %}

{% block title %}Hello {{ player.name }}!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ player.name }}! ✅</h1>

        {{ dump(context) }}

        <div id="app"></div>

    </div>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('build/css/app.css') }}">
{% endblock %}

{% block javascripts %}
    <script>
        let context = {{ context|json_encode|raw }};
    </script>

    <script src="{{ asset('build/js/main.js') }}"></script>
{% endblock %}
